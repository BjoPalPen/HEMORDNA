@page "/planering"

@* Old route, kept as a redirect so existing links and bookmarks still work - see
   docs/DESIGN.md §8. The page itself moved to /vecka (Planering.razor). *@
<Redirect To="vecka" />
