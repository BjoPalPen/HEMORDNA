# Överlämning

Lägesbild per 2026-09-18. Arbetssätt: [../CLAUDE.md](../CLAUDE.md).
Äldre lägesbilder bevaras i [handoff/](handoff/). Max 50 rader.

## Läge

`main` är pushad och kodrelease `4f913ce` deployad till https://app.hemordna.se.
Uppgiftslängd begränsas till 0–1440 minuter och nya schemadatum till fem år framåt.
Tidskrediter summeras med long och begränsas före int-konvertering.
Gamla extrema poster blockerar inte dagsplaneringen. Manuella framtida förekomster
stoppar inte normal återkommande generering. Se [SECURITY.md](SECURITY.md).
Ingen schema- eller datamigration behövs; historiska poster skrivs inte om automatiskt.

## Köra och deploya

Uppstart: [../README.md](../README.md). API `5199`, klient `5200`, Postgres `5432`.
Docker Desktop måste köra för lokal API/E2E. LAN: `--urls "http://*:PORT"`.
Server: `ssh -i ~/.ssh/hetzner_deploy deploy@62.238.45.45`, checkout `~/hemordna`.
Deploy: `git pull --ff-only origin main`, sedan
`docker compose -f docker-compose.prod.yml up -d --build --no-deps hemordna-api`.
Production kräver Resend-nyckel. Betrott proxynät: `172.19.0.0/16`; verifiera vid nätbyte.
Browserkontroll: `dotnet run --project scripts/Smoke -- https://app.hemordna.se`.

## Verifierat

Slutlig build: 0 fel/varningar. Domän: 180/180. Application: 340/340.
Full E2E-körning: 207/209; två browser-timeouts passerade vid isolerad omkörning.
Slutlig riktad E2E: 7/7, inklusive nya säkerhetsfall och båda tidigare timeouts.
Produktion: HTTPS-health Healthy, 0 containeromstarter, browserkontroll 390/1280 px.
Körande image verifierad mot den nybyggda imagen för kodrelease `4f913ce`.
Testresultat ligger lokalt i gitignorerade `TestResults/`.

## Drift och kvarstående frågor

Rollbackimage före denna release: `hemordna-api:rollback-before-4f913ce`.
Återställ vid behov genom att tagga den som `hemordna-hemordna-api:latest` och köra
Compose med `up -d --no-build --no-deps --force-recreate hemordna-api`.
Eventuell manuell rättning av gamla orimliga poster kräver backup och avsedda värden.
Dagliga kvarlämnade förekomster kan ge samma uppgiftsnamn flera gånger, avsiktligt.
Inga väntande releaseåtgärder eller godkännanden.
